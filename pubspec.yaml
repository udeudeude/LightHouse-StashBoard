name: lighthouse
version: 0.1.0+1
publish_to: none
description: An illuminated physical play surface for Looney Pyramids.

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
